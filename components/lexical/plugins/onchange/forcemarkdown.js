export default function ForceMarkdownPlugin () {
  // tbd
  // should be used to avoid mistakingly removing the markdown codeblock (like by deleting it)
  // if the user didn't switch to wysiwyg mode

  return null
}
