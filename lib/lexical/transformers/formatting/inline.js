/** underline transformer
 *
 *  rich mode: gets a text node and creates ++text++
 *
 *  markdown mode: from ++text++ to text node with underline format
 *
 */
export const UNDERLINE = {
  format: ['underline'],
  tag: '++',
  type: 'text-format'
}
