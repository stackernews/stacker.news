import { getWallet, createWallet } from 'wallets/cashu/common'

export async function testCreateInvoice ({ nwcUrlRecv }, { logger }) {

}

export async function createInvoice ({ msats, description, expiry }, { privKey, walletName, relays, mints }, { logger }) {

}
