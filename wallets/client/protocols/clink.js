export const name = 'CLINK'

export function sendPayment (bolt11, { ndebit }, { signal }) {
  // TODO(clink): implement
}

export function testSendPayment ({ ndebit }, { signal }) {
  // TODO(clink): implement
}
