export const name = 'CLINK'

export function createInvoice (
  { msats, description, expiry },
  { noffer },
  { signal }) {
  // TODO(clink): implement
}

export function testCreateInvoice (config, opts) {
  // TODO(clink): implement
}
